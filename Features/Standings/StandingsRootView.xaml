<Grid x:Class="F1Desktop.Features.Standings.StandingsRootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:F1Desktop.Features.Standings"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:StandingsRootViewModel}">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="0"/>
        </Style>
    </Grid.Resources>
    <TextBlock Grid.Column="0" Grid.Row="0" Text="Driver Standings"/>
    <ContentControl Grid.Column="0" Grid.Row="1" s:View.Model="{Binding DriverStandings}"/>
    
    <TextBlock Grid.Column="1" Grid.Row="0" Text="Constructor Standings"/>
    <ContentControl Grid.Column="1" Grid.Row="1" s:View.Model="{Binding ConstructorStandings}"/>
</Grid>
