<Grid x:Class="F1Desktop.Features.News.NewsItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:F1Desktop.Features.News"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:NewsItemViewModel}"
             Margin="10">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Image Grid.Column="0" Source="{Binding Image, Converter={StaticResource CachedUrlImage}, IsAsync=True}" 
           Width="50" 
           Height="50"
           Margin="5"/>
    <Grid Grid.Column="1">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="16" FontWeight="Bold" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="1" Text="{Binding Text}" TextWrapping="Wrap"/>
    </Grid>
</Grid>
