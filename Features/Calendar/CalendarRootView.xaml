<controls:AdonisWindow x:Class="F1Desktop.Features.Calendar.CalendarRootView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:local="clr-namespace:F1Desktop.Features.Calendar"
                       xmlns:s="https://github.com/canton7/Stylet"
                       xmlns:controls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                       xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                       xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                       xmlns:extensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                       mc:Ignorable="d"
                       d:DataContext="{d:DesignInstance local:CalendarRootViewModel}"
                       Title="Calendar"
                       Height="450"
                       Width="800"
                       ShowActivated="False"
                       ShowInTaskbar="False"
                       Focusable="True"
                       Deactivated="{s:Action FocusLost}"
                       ResizeMode="NoResize">
    <controls:AdonisWindow.TitleBarContent>
        <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
            <ToggleButton IsChecked="{Binding ShowPreviousRaces}"
                          ToolTipService.InitialShowDelay="0">
                <ToggleButton.Style>
                    <Style TargetType="{x:Type ToggleButton}"
                           BasedOn="{StaticResource {x:Static adonisUi:Styles.WindowToggleButton}}">
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <iconPacks:PackIconMaterial Kind="Rewind"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="ToolTip" Value="Hide Previous Races"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <iconPacks:PackIconMaterial Kind="RewindOutline"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="ToolTip" Value="Show Previous Races"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
        </StackPanel>
        
    </controls:AdonisWindow.TitleBarContent>
    <ItemsControl ItemsSource="{Binding Races}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border x:Name="Border"
                        BorderThickness="0"
                        Margin="0 5 0 5"
                        Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1HighlightBrush}}">
                    <ContentControl s:View.Model="{Binding}" />
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.Template>
            <ControlTemplate TargetType="ItemsControl">
                <Border BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Background="{TemplateBinding Background}">
                    <ScrollViewer CanContentScroll="False"
                                  Padding="{TemplateBinding Padding}"
                                  Focusable="False">
                        <ItemsPresenter />
                    </ScrollViewer>
                </Border>
            </ControlTemplate>
        </ItemsControl.Template>
    </ItemsControl>
</controls:AdonisWindow>