<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:F1Desktop.Controls">
    <Style TargetType="controls:ContentSwitcher">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ContentSwitcher">
                    <Grid>
                        <ContentPresenter
                            Content="{Binding DefaultContent, RelativeSource={RelativeSource TemplatedParent}}"
                            Visibility="{Binding UseAlternateContent, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource InverseBooleanToVisibility}}" />
                        <ContentPresenter
                            Content="{Binding AlternateContent, RelativeSource={RelativeSource TemplatedParent}}"
                            Visibility="{Binding UseAlternateContent, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibility}}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>